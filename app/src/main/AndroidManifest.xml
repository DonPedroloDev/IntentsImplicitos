<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tarea4">

        <!-- Activity principal: menú con los 3 puntos -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Punto A: enviar un mensaje de texto -->
        <activity
            android:name=".SendTextActivity"
            android:exported="false"
            android:label="Punto A: Enviar Mensaje" />

        <!-- Punto B: Usar una aplicacion para tomar una foto, traerla y mostrarla -->
        <activity
            android:name=".TakePhotoActivity"
            android:exported="false"
            android:label="Punto B: Tomar Foto" />
        <!--
            Punto C: nuestra app se registra como manejadora de SMS.
            Los intent-filters le dicen al sistema que esta Activity
            puede recibir intents de envío de mensajes de texto.
            exported="true" es obligatorio para que otras apps puedan llamarla.
        -->
        <activity
            android:name=".SmsHandlerActivity"
            android:exported="true"
            android:label="Punto C: SMS Handler">

            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="smsto" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

        </activity>

    </application>

</manifest>